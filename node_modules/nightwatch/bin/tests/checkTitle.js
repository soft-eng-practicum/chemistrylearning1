module.exports = {
    
   /**
   * Tests for the html body and checks title of *document
   *
   * @test
   * @param client
   */
  'Check Title Test' : function (client) {
    client
      .url('http://45.55.89.234/~xenon/chemistrylearning1/states/')
      .waitForElementVisible('body', 1000)
      .assert.title('XENON')
      .end();
  }
};